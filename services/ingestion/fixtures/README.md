# Fixture documents

Fixture PDFs used by the local seed and the end-to-end ingestion demo
(a synthetic Sonatel annual report is generated here in step 2 —
ingestion + RAG).

Real documents (BOC bulletins, annual reports, earnings releases, AGM
notices) are fetched by the worker and stored outside the repository.
